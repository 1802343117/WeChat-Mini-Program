<!--pages/ExerciseBook/exerciseBook.wxml-->
<view class="container">
  <text>练习册</text>
  <view class="nine">
    <view class="one" wx:for="{{BoxList}}" wx:key="index" data-item="{{item}}" bindtap="Shopdetail">{{item.name}}</view>
  </view>
</view>
